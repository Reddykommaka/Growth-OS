# Strict, non-hoisted node_modules is a boundary-enforcement mechanism
# (03-repository-structure.md §2): a package can only import what it declares.
hoist=false
shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true
resolution-mode=highest
save-exact=true
engine-strict=true

// @growth-os/app-link — Tracked-link redirect service (attribution spine entry point)
//
// Deployment unit, not an architectural boundary (ADR-0001).
// Apps may import a module's ./contracts only; business logic lives in packages/modules/*.
// Runtime lands in Phase 0 work item 0.8.

export {};

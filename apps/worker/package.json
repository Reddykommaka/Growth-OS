{
  "name": "@growth-os/app-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "BullMQ consumers, scheduler, outbox relay and intelligence jobs",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean && rm -rf dist"
  },
  "dependencies": {
    "@growth-os/config": "workspace:*",
    "@growth-os/logger": "workspace:*",
    "@growth-os/errors": "workspace:*"
  }
}

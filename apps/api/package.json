{
  "name": "@growth-os/app-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Fastify public REST API and provider webhook receivers",
  "scripts": {
    "build": "tsc --build",
    "test:integration": "vitest run",
    "clean": "tsc --build --clean && rm -rf dist"
  },
  "dependencies": {
    "@growth-os/config": "workspace:*",
    "@growth-os/db": "workspace:*",
    "@growth-os/errors": "workspace:*",
    "@growth-os/health": "workspace:*",
    "@growth-os/logger": "workspace:*",
    "@growth-os/telemetry": "workspace:*",
    "@growth-os/types": "workspace:*",
    "@sentry/node": "10.74.0",
    "fastify": "5.12.3",
    "pg": "8.16.3",
    "pino": "10.0.0"
  },
  "devDependencies": {
    "@growth-os/testing": "workspace:*",
    "@types/pg": "8.23.1",
    "vitest": "5.0.0"
  }
}

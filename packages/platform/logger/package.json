{
  "name": "@growth-os/logger",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "clean": "tsc --build --clean && rm -rf dist"
  },
  "dependencies": {
    "@growth-os/types": "workspace:*",
    "pino": "10.0.0"
  },
  "devDependencies": {
    "vitest": "5.0.0"
  }
}

{
  "name": "@growth-os/authn",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean && rm -rf dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@growth-os/errors": "workspace:*",
    "@growth-os/types": "workspace:*",
    "@node-rs/argon2": "2.0.2"
  },
  "devDependencies": {
    "vitest": "5.0.0"
  }
}
